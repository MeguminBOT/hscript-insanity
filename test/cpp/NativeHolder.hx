/** The Haxe counterpart of the scripted `Holder`, so the same field work can be timed compiled. */
class NativeHolder {
	public var a:Float = 0;
	public var b:Float = 1;

	public function new() {}
}
